services:
  - type: web
    name: no-mi-kos-front
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
